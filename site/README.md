# Белоснежная Избушка — анимированный сайт

Откройте `index.html` в браузере. Этот же интерфейс встроен в Android-приложение v0.2 через локальный WebView; приложение поддерживает Android 7.0+ (API 24+).
